[package]
name = "depo_cli"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4.5", features = ["derive"] }
depo_core = { path = "../core"}
dialoguer = "0.12.0"
tokio = { version = "1", features = ["full"] }
